// Base URL for the Scorechain REST API, shared by the node and the credential
// test so the host is defined in exactly one place.
export const SCORECHAIN_API_BASE_URL = 'https://api.scorechain.com/v1';
